apiVersion: v1
kind: Pod
metadata:
 name: demoapp
 labels:
 name: demoapp
spec:
 containers:
 - name: demoimage
 image: nginx
 resources:
 limits:
 memory: "128Mi"
 cpu: "500m"
 ports:
 - containerPort: 5000
